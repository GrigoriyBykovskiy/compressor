// HuffmanCode.c Define the entry point of program
//
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include "HUFFMAN.H"

int main()
{
	/*
	TNode *tmp;
	tmp = init_node(0.2, 'a');
	printf("tmp payload is %f\n", tmp->payload);
	printf("Left child is %d\n Right chils is %d\n", tmp->left_child, tmp->right_child);
	TNode* tmp_tmp;
	tmp_tmp = init_node(0.3, 'b');
	printf("tmp_tmp payload is %f\n", tmp_tmp->payload);
	add_child(tmp, tmp_tmp);
	printf("Left child is %d\n Right chils is %d\n", tmp->left_child, tmp->right_child);
	printf("tmp payload is %f\n", tmp->payload);
	*/
}
